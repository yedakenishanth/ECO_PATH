{
  "name": "ecopath",
  "version": "1.0.0",
  "description": "EcoPath — a carbon-conscious commuting web app. Static site, no build step; dependencies (Leaflet, Google Fonts) are loaded via CDN in index.html rather than installed here.",
  "private": true,
  "scripts": {
    "test": "node test.js",
    "start": "python3 -m http.server 8000"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/yedakenishanth/ECO_PATH.git"
  }
}
